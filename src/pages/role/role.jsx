import React, { Component } from 'react'

import './role.less'

class rule extends Component {
    render() {
        return (
            <div>
                rule
            </div>
        );
    }
}

export default rule;